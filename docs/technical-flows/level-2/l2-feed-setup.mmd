%% Level 2 — notification feed setup (discovery stub)

sequenceDiagram
  actor Partner as Partner user
  participant App as Partner portal
  participant API as Platform API
  participant Store as Feed configuration

  Partner->>App: Define feed channel and auth
  App->>API: Save feed definition
  API->>Store: Persist channel + auth metadata
  Store-->>API: OK
  API-->>App: Feed ID / status
  App-->>Partner: Confirmation
